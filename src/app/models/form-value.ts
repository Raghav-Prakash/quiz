/**
 * Form structure that determines the value the home component form group
 * contains.
 */
export interface FormValue {
  amount: number;
  category: string;
  difficulty: string;
  type: string;
}
